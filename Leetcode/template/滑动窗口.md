边移动边更新状态->一边扩一边缩
### 一般用于解决：
- 数组
- 字符串
- 连续子数组
- 连续子串

### 经典例题

- 求最小子数组
## 代码模板

```java
int left = 0;

for (int right = 0; right < nums.length; right++) {

    // 1. 右边进入窗口
    // update...

    while (窗口不合法) {

        // 2. 左边离开窗口
        // update...

        left++;
    }

    // 3. 更新答案
}
```